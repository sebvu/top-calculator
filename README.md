# TOP calculator

This is my implementation of TOP's Calculator project.

- impenetrable logic! i think
- full keyboard support! 
    - `=` is Enter and `POP` is Backspace
    - `+/-` and `CLEAR` have no keyboard bindings, i lied
    - everything else has bindings
